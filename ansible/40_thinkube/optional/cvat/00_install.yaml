---
# ansible/40_thinkube/optional/cvat/00_install.yaml
# Description:
#   Orchestrator playbook for CVAT installation
#   Deploys CVAT computer vision annotation platform with OAuth2 authentication
#
# Requirements:
#   - Kubernetes (k8s-snap) must be installed and running
#   - Keycloak must be deployed for OAuth2 authentication
#   - Harbor registry must be available
#   - PostgreSQL core service must be deployed
#   - Valkey core service must be deployed
#   - ClickHouse must be deployed
#
# Usage:
#   cd ~/thinkube
#   ./scripts/run_ansible.sh ansible/40_thinkube/optional/cvat/00_install.yaml
#
# 🤖 [AI-generated]

- name: CVAT - Deploy
  import_playbook: 10_deploy.yaml

- name: CVAT - Configure Service Discovery
  import_playbook: 17_configure_discovery.yaml