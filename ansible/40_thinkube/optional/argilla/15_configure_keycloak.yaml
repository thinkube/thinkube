---
# ansible/40_thinkube/optional/argilla/15_configure_keycloak.yaml
# Description:
#   Placeholder for Keycloak configuration
#   The OAuth2 Proxy role handles all Keycloak integration
#
# Requirements:
#   - Keycloak must be deployed and accessible
#   - Argilla must be deployed
#
# Usage:
#   cd ~/thinkube
#   ./scripts/run_ansible.sh ansible/40_thinkube/optional/argilla/15_configure_keycloak.yaml
#
# 🤖 [AI-generated]

- name: Configure Keycloak for Argilla
  hosts: microk8s_control_plane
  gather_facts: true

  tasks:
    - name: Display information
      ansible.builtin.debug:
        msg:
          - "Keycloak configuration for Argilla is handled by the OAuth2 Proxy role"
          - "This is included in the main deployment playbook (10_deploy.yaml)"
          - "No additional Keycloak configuration is needed"