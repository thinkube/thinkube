---
# ansible/40_thinkube/optional/litellm/00_install.yaml
# Description:
#   Orchestrator playbook for LiteLLM installation
#   Deploys LiteLLM proxy server with OIDC authentication
#
# Requirements:
#   - Kubernetes (k8s-snap) must be installed and running
#   - Keycloak must be deployed for OIDC authentication
#   - Harbor registry must be available
#
# Usage:
#   cd ~/thinkube
#   ./scripts/run_ansible.sh ansible/40_thinkube/optional/litellm/00_install.yaml
#
# 🤖 [AI-generated]

- name: LiteLLM - Configure Keycloak OIDC
  import_playbook: 10_configure_keycloak.yaml

- name: LiteLLM - Deploy
  import_playbook: 11_deploy.yaml

- name: LiteLLM - Configure Service Discovery
  import_playbook: 17_configure_discovery.yaml