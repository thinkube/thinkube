# Managed by Thinkube — do not edit.
# One check of the worker's physical link; driven by thinkube-link-watchdog.timer.
# Escalation state lives in /run and is therefore reset by the reboot the
# script itself may trigger.
[Unit]
Description=Thinkube: worker link watchdog (single check)

[Service]
Type=oneshot
ExecStart=/usr/local/bin/thinkube-link-watchdog
