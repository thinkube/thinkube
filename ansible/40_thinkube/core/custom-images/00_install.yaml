---
# ansible/40_thinkube/core/custom-images/00_install.yaml
# Description:
#   Orchestrator for building all custom images
#   Currently only includes Jupyter images
#
# Requirements:
#   - Harbor registry must be running
#   - Podman must be available
#
# Usage:
#   cd ~/thinkube
#   ./scripts/run_ansible.sh ansible/40_thinkube/core/custom-images/00_build_all.yaml
#
# 🤖 AI-assisted

- name: Build Jupyter Images
  import_playbook: 10_build_jupyter_images.yaml