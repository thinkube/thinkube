#!/bin/bash

# Copyright 2025 Alejandro Martínez Corriá and the Thinkube contributors
# SPDX-License-Identifier: Apache-2.0

set -e

echo '===== Installing VS Code Extensions ====='

# Ensure extensions directory exists
mkdir -p /home/coder/.local/share/code-server/extensions

# Extensions directory
EXTENSION_DIR="/home/coder/.local/share/code-server/extensions"

# Install thinkube-ai-integration extension from cloned repo
if [ -d "/home/coder/thinkube-platform/thinkube-ai-integration" ]; then
  echo 'Building thinkube-ai-integration extension...'
  cd /home/coder/thinkube-platform/thinkube-ai-integration
  
  # Pull latest changes
  git pull || echo "Could not pull latest changes"
  
  # Build extension
  npm install --production=false
  npm run compile
  
  # Install by creating symlink
  rm -rf "$EXTENSION_DIR"/thinkube.thinkube-ai-integration-*
  ln -sf "/home/coder/thinkube-platform/thinkube-ai-integration" "$EXTENSION_DIR/thinkube-ai-integration"
  
  echo '✅ thinkube-ai-integration extension installed via symlink'
else
  echo '❌ thinkube-ai-integration repository not found at /home/coder/thinkube-platform/thinkube-ai-integration'
fi

# Install thinkube-cicd-monitor extension from cloned repo
if [ -d "/home/coder/thinkube-platform/thinkube-cicd-monitor" ]; then
  echo 'Building thinkube-cicd-monitor extension...'
  cd /home/coder/thinkube-platform/thinkube-cicd-monitor
  
  # Pull latest changes
  git pull || echo "Could not pull latest changes"
  
  # Build extension
  npm install --production=false
  npm run compile
  
  # Install by creating symlink
  rm -rf "$EXTENSION_DIR"/thinkube.thinkube-cicd-monitor-*
  ln -sf "/home/coder/thinkube-platform/thinkube-cicd-monitor" "$EXTENSION_DIR/thinkube-cicd-monitor"
  
  echo '✅ thinkube-cicd-monitor extension installed via symlink'
else
  echo '❌ thinkube-cicd-monitor repository not found at /home/coder/thinkube-platform/thinkube-cicd-monitor'
fi

echo 'VS Code extensions installation complete'